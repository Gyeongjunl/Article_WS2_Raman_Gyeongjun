# Handoff — main.tex work, 2026-08-04

Context for continuing this in a new conversation. Next task: **properly write/revise
main.tex, with GJ reviewing each change before it's applied** (not auto-editing).

## Ground rule for next session
Propose changes first (diff/description), wait for approval, then edit. Do not edit
main.tex or figures directly without confirmation first.

## What's already in main.tex (as of this session)
- Figure 1: done, untouched, uses `figures/fig1.png` (schematic + optical image +
  Raman RT spectrum + PL 5K spectrum). No issues.
- Figure 2: `\includegraphics{figures/fig2.png}`, 3 panels — (a) non-encap 2D Raman
  map (T on x, shift on y), (b) encap 2D Raman map (same orientation), (c) A1g(Γ)
  position vs T, both samples overlaid (blue=encap, red=non-encap) with linear fits.
  - **Layout currently under dispute**: was 1×3 (too small when squeezed to
    \linewidth). Changed to 2-row layout ((a)(b) top, (c) bottom-centered, same
    size as top panels) via `compose_figures.py` — but this was done without
    asking first, which GJ was unhappy about. Current `figures/fig2.png` reflects
    the 2-row version. Confirm with GJ whether to keep, revert to 1×3, or try
    another layout (options offered: (a) top/(b)(c) bottom; vertical 3-stack;
    split (c) into its own figure) before touching again.
  - Numbers used: non-encap slope −0.0138 ± 0.0002 cm⁻¹/K, intercept 417.95 cm⁻¹,
    R²=0.995 (T=298–1223K, fit T≥100K). Encap slope −0.0121 ± 0.0001 cm⁻¹/K,
    intercept 419.55 cm⁻¹, R²=0.997 (T=5–1273K data, fit T≥100K).
- Figure 3: `\includegraphics{figures/fig3.png}`, 2 panels — (a) PL 3D waterfall
  (6 representative spectra, 5–1023K), (b) E0(T) Voigt data + Pässler fit with
  parameter box. Trimmed down from the original 4-panel plan (FWHM/η/intensity
  panels dropped from the main figure; text now points to Supporting Info Fig. S5
  instead).
  - Pässler params used: E0(0)=2.0491±0.0036 eV, α=(3.741±0.069)×10⁻⁴ eV/K,
    Θ=280.1±28.8 K, p=4.04±2.23, R²=0.9964, RMSE=7.9 meV.

## Unresolved / flagged for GJ decision
1. **"Encapsulation-independent" claim conflict.** Original draft's abstract/
   discussion/conclusion claimed the Raman A1g slope is identical regardless of
   encapsulation. Actual fitted data shows encap slope (−0.0121) is ~12% smaller
   in magnitude than non-encap (−0.0138). Text was softened this session to
   "comparable, though not identical" throughout (abstract, Results §3.1.2,
   Discussion, Conclusion) and a `% TODO(GJ)` comment was left in main.tex right
   after the Fig. 2(c) paragraph (search for `TODO(GJ)`). **Needs GJ's call**: is
   this a real physical effect worth discussing, or within experimental/fitting
   error and better reverted to "independent" framing?
2. **Figure 2 layout** (see above) — not yet confirmed.
3. E2g+2LA doublet slope: no fit was done this session (only A1g). The old
   `[TBD]` sentence about it was deleted rather than filled. Add back later if
   that fit gets done.
4. Non-encap upper T bound: text now says "1223 K" (last valid A1g fit point)
   instead of "1273 K" in a couple of spots — the 1273.15K row in
   `fit_raman_v4__A1g.dat` has no A1g value (NaN). Worth double-checking this is
   the correct interpretation (dissociation vs. just a missing fit).

## Key files
- `Article_WS2_Raman_Gyeongjun/main.tex` — the manuscript.
- `Article_WS2_Raman_Gyeongjun/figures/fig2.png`, `fig3.png` — composed figures.
- `Article_WS2_Raman_Gyeongjun/compose_figures.py` — script that builds fig2/fig3
  from the individual panel PNGs (paths documented in its docstring). Panel
  source files:
  - Fig2(a): `Analysis/Raman_non_encap/output/figures/plot_raman__2Dmap_zoom_bgsub_swapped.png`
  - Fig2(b): `Analysis/Raman_encap/output/figures/plot_raman__2Dmap_zoom_swapped.png`
  - Fig2(c): `Analysis/Raman_encap/output/figures/fit_A1g__position_vs_T_encap_vs_nonencap.png`
  - Fig3(a): `Analysis/PL_encap/output/figures/plot_PL_3D_waterfall__waterfall.png`
  - Fig3(b): `Analysis/PL_encap/output/figures/plot_PL__E0_model_Passler.png`
- `Article_WS2_Raman_Gyeongjun/figure_proposition.md` — original figure plan
  (pre-dates this session's actual final panel choices; Figure 3 ended up
  simpler than planned there — 2 panels instead of 4).
- `Article_WS2_Raman_Gyeongjun/Figures for an article raman 20260804.pptx` — GJ's
  own figure-composition deck; slide notes on the "(v3)" slides for Fig 2 and
  Fig 3 confirm which panel PNGs are the finalized choices.

## Verification done
Full main.tex compiled cleanly with pdflatex (biblatex/mhchem/chemformula stubbed
out locally since not installed in this sandbox — real packages are still declared
in the actual main.tex, untouched). 8 pages, no errors, one negligible 2.4pt
overfull hbox. All figures render correctly in the PDF.
